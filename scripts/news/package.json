{
  "name": "peaceful-pursuit-news-pipeline",
  "private": true,
  "version": "1.0.0",
  "description": "Scheduled fetch + AI rewrite pipeline for the Cool News section. Runs in CI only, never shipped to the site.",
  "type": "module",
  "scripts": {
    "start": "node run.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.1.0",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
